export interface ActionItem {
  id: string;
  title: string;
  description: string;
}

export interface TrelloBoard {
  id: string;
  name: string;
}

export interface TrelloList {
  id: string;
  name: string;
}